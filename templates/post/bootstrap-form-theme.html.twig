{% block form_row %}
    <div class="form-group">
        {{form_label(form)}}
        {{form_errors(form)}}
        {{form_widget(form, { 'attr': { 'class': 'form-control' } })}}
        <small class="text-muted">
            {{form_help(form)}}
        </small>
    </div>
{% endblock %}